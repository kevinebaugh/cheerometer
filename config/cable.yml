development:
  adapter: async

test:
  adapter: test

production:
  # Use async adapter for single-instance deployments (no Redis needed)
  # If you scale to multiple instances, switch back to redis adapter
  adapter: async
  # adapter: redis
  # url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  # channel_prefix: cheerometer_production
